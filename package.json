{
  "name": "mindful-commons",
  "version": "1.0.60",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pub": "git add . && git commit -m\"Auto-prepublish-commit\" && tsc && npm version patch && npm publish"
  },
  "author": "Wideyedwonderer",
  "license": "ISC",
  "files": [
    "dist/**",
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^14.x",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "@nestjs/core": "^7.5.1",
    "@nestjs/common": "^7.6.5",
    "@nestjs/config": "^0.6.2",
    "@nestjs/microservices": "^7.6.5",
    "class-transformer": "^0.3.2",
    "class-validator": "^0.12.2",
    "rxjs": "^6.6.3"
  }
}
